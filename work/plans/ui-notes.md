# 口播 UI 笔记（已换算到当前 v4 时间线）

用户的原始笔记时间戳写于观看 **v2** 时（开头 43.64s 还没剪掉），笔记里引用的字幕 JSON 片段
则是 **v4** 的。此文件把两者统一换算到当前时间线，避免每轮重新推导。

- 当前视频：`work/base-cut-v4-audio-light-v1.mp4`（714.653s）
- 当前字幕：`work/subtitles/base-cut-v4-audio-light-v1-clean-v1.json`
- 当前 UI：`work/overlays/overlays-v1.json`
- 换算工具：`python3 work/scripts/convert-note-time.py v2 08:23 09:12`
- 空隙工具：`python3 work/scripts/find-subtitle-gaps.py 440 545`

```
v2 -> v4 : 去掉开头 0-43.64   (v4 = v2 - 43.64)
v4 -> v7 : 再去掉开头 0-1.02  (v7 = v4 - 1.02，加速段除外)
           + 420.0-507.29 段 5 倍速
```

> **当前时间线是 v7**，不是 v4。本页表格里的「v4 时间」一列减 1.02 才是 Studio 里看到的时间
> （加速段 420 之后另算，见 `work/timeline-map-v7.json`）。v5/v6 是中间产物，已删。
> 完整规则见 `work/README.md`。

## 逐条对照

| 原始笔记（v2） | v4 时间 | 落点（字幕原文） | 状态 |
|---|---|---|---|
| `00:00` 标题 | `0.4-9.0` | 开场 | 已做 · `title` |
| 讲 agent 类型时加 UI，两类：带 GUI / 不带 GUI（TUI） | `64.9-88.5` | "例如腾讯的Workbuddy" → "没有交互界面的" | 已做 · `agent-types` |
| 字幕 `67.88` "那是另外一类AI Agent" | `67.88` | 同左 | GUI 框已于 64.9 出现 |
| 字幕 `83.98` "没有交互界面的" —— 原笔记写「消失」 | `83.98` | 同左 | **改为 TUI 框开始高亮**，整组 88.5 收 |
| `03:54-03:58` 更正：大模型能力影响挺大 | `190.36-194.36` | "这个问题其实不大的" / "OK 这是第一个前提" | 已做 · `correction-model`（用户调为 189-198） |
| `04:07-04:10` 给一个固定的文件夹 | `203.36-206.36` | "你需要有一个稳定的地方" | 已做 · `stable-folder`（用户调为 203.36-214） |
| `04:29-04:56` 模糊编辑器右边（露了 user.md） | `225.36-252.36` | "在这里我放了user.md" → "你就给他什么样的讯息就好了" | 已做 · `blur-user-md`，**另加了左下角 OUTLINE 面板** |
| `05:50-05:54` 让 agent 具备联网/外部能力 | `306.36-310.36` | "就是你让这个Agent有联网的能力" | 已做 · `web-access`（用户调为 306.36-318） |
| `08:23-09:12` 这里可以加速 2 倍 | `459.36-508.36` | 等待 agent 调研的空档 | **已撤销**，见下 |
| `11:50` 总结 UI（两点） | `666.36` | "就是给他一个稳定的可以存东西的地方" | 已做 · `summary` `664.28-691.76`，第 2 点 `679.66` |

## 加速段：撤销的原因与重做参考

笔记要求加速 v2 `08:23-09:12`（v4 `459.36-508.36`）。v5 做过一版，用户认为范围不对，已撤销。

若要重做，`find-subtitle-gaps.py 440 545` 显示这一段的可切点只有三处：

| 空隙 | 位置 | 说明 |
|---|---|---|
| `456.88 → 466.88`（10s） | 07:36.88 | 最干净的起点 |
| `488.88 → 496.88`（8s） | 08:08.88 | v5 用的终点 |
| `524.88 → 526.04`（1.16s） | 08:44.88 | 长句之后 |

关键约束：`496.88-524.88` 是**一整句 28 秒**的叙述（"他一直在看新文章…"），笔记要的
`508.36` 正落在它中间，切下去会变成半句原速半句 2 倍速。要么在 `496.88` 停（v5 的做法，
但那 28 秒仍是 1 倍速），要么把整句一起加速到 `524.88`。

## 需要留意的偏差

1. **标题落在被剪掉的开头**。笔记的 `00:00` 是 v2 的 0 秒，那段已在 v4 剪掉。标题现在压在
   v4 的 0.4-9.0s，也就是"其实我真正用AI Agent也是从OpenClaw（龙虾）"这句上。

2. **"消失"改成了"高亮"**。原笔记要 UI 在 `83.98`（"没有交互界面的"）消失，但后续要求是
   "出现时间更长一点"。两条冲突，取后者：那句话现在是 TUI 框的高亮点题时刻。

## 下一轮怎么报时间

在 Remotion Studio 里看到的时间就是 v4 时间，直接报即可，不需要换算。
